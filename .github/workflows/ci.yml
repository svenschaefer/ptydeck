name: CI (disabled for remote runners)

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Remote CI is disabled
        run: echo "Remote CI is disabled. Use local scripts instead."
