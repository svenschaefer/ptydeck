# CODEX CONTEXT - ptydeck

Last updated: 2026-04-08 (H55 slash-command parity delivered; H56 quality hardening delivered; H57 guided management/settings UX closeout delivered; H58 guided SSH trust and progressive-disclosure settings closeout delivered; H59 quality and coverage follow-up delivered; H60 per-session send history delivered; H61 send-history hardening delivered; H62 multi-device terminal-control foundation delivered on `feature/h62-multi-device-control-foundation`; H63 trusted-local multi-device identity and handoff hardening is now also delivered on that branch for the single-user local-network case; H64 trusted-local multi-device rollout hardening is now also delivered on that branch, covering deterministic rollback and restore from the pre-H62 startup backups, explicit trusted-local device rename and stale-device cleanup UX, and immediate reclaim affordances on blocked write paths; H65 remains active on that branch only for the real second-client LAN validation task after its runbook and acceptance-checklist slice was delivered; H66 is now also delivered on that branch as the trusted-local fast handoff and device-layout-recall wave, eliminating release-first takeover behavior, making take/reclaim idempotent and scope-aware across all sessions/deck/session, adding a subtle startup plus anytime takeover UX, and automatically applying device-local layout and terminal-size preferences when control moves between desktop and laptop form factors; H67 is now also fully delivered on that branch as the repo-wide quality and coverage hardening wave, closing the trusted-local runtime/controller/file-transfer/custom-command frontend hotspots plus the earlier backend hotspot slices while H65 remains externally blocked on second-client validation; Workspace Library panels now stay scrollable inside the dialog shell; `local` connection drafts hide SSH-only controls; guided SSH trust now uses backend SSH host-key probing plus fetch/review/trust flow instead of manual raw key entry; session and management dialogs now use `Advanced` disclosure for low-level fields; non-open dialogs are now explicitly forced back to `display:none` so dialog layout classes cannot leave closed dialogs visible; terminal-local paste now uses a native-helper paste path with a timed clipboard fallback for xterm instances that never emit the follow-up paste event; guarded terminal paste auto-reveals the confirmation UI and now also surfaces the guard summary immediately when strict per-session send-safety holds the paste for confirmation; guarded composer sends now also surface explicit “nothing has been sent yet” feedback when send safety holds multiline or other risky input for confirmation, and the command-guard UI now uses a stronger `Send anyway` action plus immediate focus on that primary confirm action; the multi-device terminal-control requirement baseline is now explicit: each session has exactly one Owner, zero or one Controller, and zero or more read-only Spectators; control is single-writer and exclusive per session; PTY resize authority is bound to the active Controller or a stable host default when no Controller exists; shared runtime state is separated from client-local layout/focus/search state; and control metadata such as connected clients, current Controller, last input sender, and control-transfer timestamps must be observable; the H62 foundation is now implemented on the feature branch: the backend exposes normalized `session.controlState` metadata plus top-level WebSocket `clientId`, authenticates operator REST writes with `x-ptydeck-client-id`, and enforces controller-only PTY input and resize with explicit take/release/transfer control endpoints; the frontend now renders session control badges and take/release/transfer UI, blocks composer sends, terminal-local input/paste, settings apply, and PTY resize for non-controllers, and preserves a deterministic implicit local-owner fallback until a concrete control attachment exists so single-client boot behavior does not regress; the trusted-local H63 follow-up is now also delivered on that branch: the frontend persists a stable browser/device identity and sends it through the WS-ticket flow, the backend tracks attached clients independently of transient WebSocket connection ids, controller ownership survives bounded offline windows for reconnect/reclaim, stale client attachments expire deterministically before reassignment, and the UI now explains attached devices, reconnect-pending state, reclaim affordances, and multi-tab counts without expanding product-level role management beyond controller-versus-read-only behavior; the H64 rollout-hardening follow-up is now also delivered on that branch: backend and browser-local startup backups can now be restored deterministically through explicit tooling and a restore page, the control UI now supports trusted-local device rename and stale-device forget flows, and blocked send/paste/resize paths now surface immediate `Take Control` / `Reclaim Control` actions instead of passive read-only failures; the H65 documentation slice is now also delivered on that branch: `DEPLOYMENT.md` includes a second-client LAN smoke checklist under the real hostnames plus explicit merge-readiness acceptance criteria for startup-backup verification, trusted-local identity persistence, reclaim behavior, stale-device cleanup, and rollback-to-`main`; the H66 fast-handoff closeout is now also delivered on that branch: trusted-local takeover is no longer owner-gated or release-first, the backend exposes a top-level scope-claim endpoint for `all` / `deck` / `session`, blocked write paths now support `Take Control and Retry`, and successful trusted-local takeover now captures or reapplies device-local layout baselines automatically; the H67 closeout now also delivered direct frontend coverage and light hardening for trusted-local handoff/runtime storage, modal and session-card UI controllers, browser-local startup backup verification, file-transfer normalization, and the custom-command model so the whole repo gate stays deterministic; the H62 feature branch still has a hard startup rollback gate: backend startup must first create or verify a sidecar backup manifest plus persisted-data copy, frontend startup must first create or verify a browser-local pre-H62 snapshot of settings and other local state, and the feature branch must not finish booting if either backup cannot be created or later validated; send-safety settings now expose an explicit non-heuristic `confirmOnAnyInput` / `Always confirm before send` catch-all, while the other send-safety toggles are intentionally described as heuristic-, pattern-, or threshold-based rules instead of an implicit confirm-everything mode; xterm viewport scrollbar dragging now has a terminal-local drag bridge on the real scrollbar gutter so drag still works when native gutter targeting is unreliable under xterm render layers; the left sidebar `Find`, `Terminal Size`, and `Saved Layouts` sections are now independently collapsible and persist their collapsed state through the existing local layout settings store; restored terminals now get explicit refresh plus fonts-ready reflow stabilization; visible terminals now also run a local mount-stabilization pass after hard reload so the first screen draw does not wait for later interaction; late snapshot-replay retries now trigger the same per-session stabilization pass for terminals that only receive replayed output after a delayed mount; the session header now also exposes a manual terminal-refresh action next to the terminal options icon, and that button intentionally reuses the same local-only stabilization path (force local resize with `skipRemote`, viewport refresh, scroll sync) instead of emitting extra backend resize traffic; the control pane now also exposes a per-session `History` entry point backed by frontend-local bounded persistence, searchable summary rows, explicit long-entry expansion, and restore-to-input behavior for previously sent composer payloads without auto-sending them; the send-history follow-up hardening now adds visible local-persistence disclosure, `Delete Selected` / `Clear Session` hygiene controls, non-empty-draft overwrite confirmation on restore, a session-pinned dialog model with explicit `Use Current Session` switching, and compact clamped summary rows for long payloads; H59 replaced the last targeted browser `prompt()` / `confirm()` flows in deck actions, saved layouts, and session delete confirmation with a reusable modal-backed action dialog; the backend SSH reconnect/offline quality-gate flake is now stabilized by decoupling the integration test from the reconnect stabilization timer; `CHANGELOG.md` owns completed release history.)

Documentation sync status: repository markdown files are aligned on 2026-04-08. `TODO.md` contains open concrete tasks only, `ROADMAP.md` contains only active and queued execution order plus dependencies, `CHANGELOG.md` contains completed and validated release history, and `TODO-OUTLOOK.md` contains only future epics plus deferred explicit backlog.

## Current Documentation Contract

- `TODO.md`: open concrete tasks only.
- `ROADMAP.md`: active and queued waves only; ordering, versions, and dependencies.
- `CHANGELOG.md`: completed and validated release history.
- `TODO-OUTLOOK.md`: future epics and deferred explicit backlog only.
- `CODEX_CONTEXT.md`: persistent architecture, process, and governance context only.

## Ownership Model

- `SAS`: final decision authority.
- `CODY`: documentation and coordination owner.
- `BE`: backend implementation owner.
- `FE`: frontend implementation owner.
- `PLAT`: platform/runtime owner.
- `QA`: quality and test owner.

## Current Delivery State

- The only active release wave on `feature/h62-multi-device-control-foundation` is `v0.4.0-H65`.
- `MDT-014` remains the only open scoped task in `H65`, and it still requires the real second-LAN-client validation pass under the production-like local hostnames.
- `H67` is now fully delivered on the branch: the backend hotspot slices (`QLT-189`, `QLT-190`) and the frontend/QA closeout slices (`QLT-191`, `QLT-192`, `QLT-193`, `QLT-194`) are complete, and the branch-local code quality gate is back to a deterministic green state.
- Completed wave history is intentionally no longer duplicated across planning documents; it lives in `CHANGELOG.md`.

## Architecture Baselines To Preserve

- Frontend runtime no longer uses the old stream-scanning/plugin/notification path for normal activity handling; keep raw terminal streaming plus visible-output-based active/inactive detection only.
- Command surface model is intentionally consistent: `>` for quick switching, `@<sessionSelector>` for explicit direct-target slash routing, and `/` for the slash-command plane.
- Session notes are persisted and multiline; the session header shows a first-line preview with truncation and tooltip access to the full note.
- Quick-ID swap ordering is backend-persisted and shared across reload, reconnect, and restart restore.
- Send safety is configured through explicit per-session `inputSafetyProfile` option fields, not presets.
- Send safety is also intentionally not a monolithic "strict mode". The baseline now exposes one explicit catch-all guard (`confirmOnAnyInput`) for operators who want every non-empty send to require confirmation; the remaining send-safety toggles are narrower structural, heuristic, pattern-based, or threshold-based checks and should keep being labeled that way in the UI and slash/help surfaces.
- Session settings are tabbed; terminal-surface `Ctrl-C` ambiguity is resolved through a local copy-versus-cancel prompt.
- Remote session baseline exists for `local` and `ssh`, including remote auth metadata, SSH host-key trust persistence, backend SSH host-key probing, reconnect metadata, and saved connection profiles.
- Read-only sharing exists for session/deck spectator access.
- The multi-device terminal-control requirement baseline is now fixed at the document level in `docs/Multi-Device Terminal Control Requirements.md`: each session has exactly one Owner, zero or one Controller, and zero or more Spectators; control is single-writer and deterministic; PTY resize authority belongs only to the active Controller or a stable host default when no Controller exists; globally synchronized runtime state is intentionally narrower than client-local layout/focus/search state; and controller/observer metadata must remain visible to attached clients.
- The first near-term implementation wave for that baseline is now delivered on `feature/h62-multi-device-control-foundation`: runtime role/control state, controller-only input and resize enforcement, visible control metadata plus take/release/transfer UI, browser-side read-only enforcement for non-controllers, and multi-client regression coverage now exist on top of the existing authenticated single-host runtime. Broader auth-provider, tenant-isolation, and audit-log work remains deferred in `TODO-OUTLOOK.md`.
- The trusted-local H63 follow-up is now delivered on `feature/h62-multi-device-control-foundation`: stable client identity survives reload/reconnect, stale attached clients expire deterministically, controller reclaim/handoff is predictable for the same single user across devices/tabs, and the UI stays device-oriented rather than introducing broader product-level role management.
- The trusted-local H64 rollout-hardening wave is now also delivered on `feature/h62-multi-device-control-foundation`: the branch now has deterministic rollback and restore tooling for the pre-H62 startup backups, explicit trusted-local device rename and stale-device cleanup flows, and immediate `Take Control` / `Reclaim Control` affordances when a blocked write originates from a non-controller local device.
- The active branch-local trusted-local step is `v0.4.0-H65`: this wave is intentionally validation- and rollout-focused rather than architectural. Its checklist/runbook slice is already documented, and the only remaining closure item is the real second-client LAN verification under the production-like local hostnames, including startup takeover prompting, scope-aware control claim, and automatic device-local layout recall on takeover.
- The trusted-local H66 fast-handoff follow-up is now also delivered on `feature/h62-multi-device-control-foundation`: trusted-local takeover is now single-user operator-lease based instead of release-first, server-side scope claims cover `all sessions`, `current deck`, and `current session`, blocked send/paste/resize flows can take control and retry immediately, and successful takeover now reapplies or first-captures per-device local layout and terminal-size baselines without turning layout state into shared runtime state.
- The H67 hardening wave on that branch came directly from a fresh full-gate coverage review on 2026-04-08: it targeted backend hotspots in `validation.js`, `startup-backup.js`, `persistence.js`, `proxy.js`, `session-manager.js`, and `ssh-host-key-probe.js`, plus frontend hotspots in `trusted-local-handoff-runtime-controller.js`, `trusted-local-layout-runtime-controller.js`, `ws-client.js`, `startup-backup-runtime-controller.js`, `ui/action-dialog-controller.js`, `ui/deck-actions-controller.js`, `ui/session-card-interactions-controller.js`, `file-transfer-runtime-controller.js`, `custom-command-model.js`, `split-layout-runtime-controller.js`, `layout-profile-runtime-controller.js`, and `workspace-preset-runtime-controller.js`.
- The delivered `QLT-189` slice now hardens startup-backup manifest safety directly: existing manifests must match the expected source path and deterministic payload backup path, `sourceExisted=true` manifests must carry a non-empty payload path, and `sourceExisted=false` manifests must not carry a stray payload path. Direct tests now also cover manifest verification failure, restore verification mismatch, encrypted persistence failure modes, temp-file cleanup on persistence rename failure, and additional request-validation rejection branches.
- The delivered `QLT-190` slice now closes the remaining backend hotspot trio without changing runtime contracts: proxy coverage now includes ipv4-mapped trusted-proxy list normalization plus invalid forwarded-header fallback, session-manager coverage now proves that deleting a degraded SSH session clears pending reconnect timers so the session cannot respawn after deletion, and SSH host-key probe coverage now includes malformed keyscan-line filtering and timeout-via-killed-process error mapping.
- The delivered `QLT-191` through `QLT-194` slices now close the remaining frontend hotspot wave without changing trusted-local runtime contracts: trusted-local handoff/layout/storage helpers now reject malformed scope/layout/backup inputs more deterministically, `ws-client.js` no longer stacks reconnect timers or loses synchronous constructor failures, prompt-free UI controllers have direct coverage across dismiss/error/fallback branches, file-transfer validation now normalizes whitespace-only paths before transport, and the custom-command model now has direct parsing/normalization/rendering coverage for the long-tail ambiguity branches that previously sat below the repo’s quality baseline.
- Multi-device control metadata is now part of the standing runtime contract on this branch: REST and WebSocket session payloads expose normalized `session.controlState` (`owner`, `currentController`, `controllerClientId`, `controllerChangedAt`, `lastInput`, and `attachedClients`), the snapshot also exposes the local client's top-level WebSocket `clientId`, and authenticated REST writes identify the operator through the `x-ptydeck-client-id` header so backend arbitration can remain deterministic across multiple attached clients.
- Browser-side write enforcement now intentionally mirrors backend arbitration: composer sends, terminal-local input/paste, session settings apply, and PTY resize paths must check session control state before transport, while the UI surfaces the current control holder plus explicit take/release/transfer actions instead of silently failing writes from non-controller clients.
- Until a concrete session-control client attachment exists, the frontend intentionally preserves an implicit local-owner fallback for normal single-client boots. Once attached-client metadata exists, write authority is determined only by the explicit multi-device control model rather than by legacy single-client assumptions.
- The H62 feature branch must remain rollback-safe on first boot: backend startup now has to create or verify a sidecar backup of the persisted runtime data file before continuing, and frontend startup now has to create or verify a browser-local snapshot of pre-H62 settings/state before bootstrapping the app. If either backup is unavailable, incompatible, unreadable, or unverifiable, startup must fail instead of partially migrating runtime or local state.
- Session-scoped file transfer exists through the bounded backend contract and `/transfer` workflows.
- Controlled mouse forwarding exists as per-session `mouseForwardingMode` (`off|application`), defaulting to `off`.
- Terminal-local paste handling must remain explicit and per-terminal: do not rely only on the browser `paste` event. The delivered baseline now intercepts `Ctrl/Cmd+V`, `Shift+Insert`, `beforeinput` `insertFromPaste`, and clipboard/middle-click paste at both the mounted terminal and the xterm helper textarea/input layer, routes them through the same guarded `onTerminalPaste` path, suppresses duplicate follow-up clipboard events, and also uses the xterm custom key-event hook to stop one terminal from falling back to leaking raw `^V` when the browser-level paste path is bypassed.
- Terminal-local keyboard paste must remain native-first but not native-only: the current delivered fix uses the xterm custom key hook to intercept `Ctrl/Cmd+V` / `Shift+Insert`, records a short-lived pending paste source, prefers the subsequent helper-textarea `beforeinput` / `paste` event for the real payload, and falls back to explicit `readClipboardText()` only if that follow-up event never arrives for the mounted terminal. This preserves the browser-native paste path when available while still covering the broken-terminal case where shortcut interception would otherwise yield raw `^V` or no paste at all.
- Guarded terminal paste must reveal the confirmation surface before staging a pending send. The control-pane body can be hidden, so any send-safety path that stores a pending terminal paste must also call the control-pane `show()` path; otherwise the operator can click paste and see nothing happen even though the paste is waiting for confirmation.
- Session-local paste behavior is intentionally allowed to differ by `inputSafetyProfile`, because strict sessions can hold multiline or recent-target-switch input for confirmation while permissive sessions send it directly. That difference must never be silent: terminal-local guarded paste must surface the returned guard summary immediately so a strict session reads as "confirmation required" rather than "paste is broken".
- The same clarity rule now applies to composer-driven guarded sends as well: if send safety holds multiline or other risky input for confirmation, the default feedback surface and the guard panel must both state that nothing has been sent yet, and the primary action must read as an explicit override (`Send anyway`) rather than a vague secondary label.
- Terminal scrollbar dragging must preserve xterm's native viewport contract: keep `.terminal-mount .xterm-viewport` on `overflow-y: scroll` with a stable scrollbar gutter instead of switching it to `auto`, because the auto-scroll override can leave the gutter visually present but not reliably draggable while wheel scrolling still works.
- Native gutter targeting under xterm render layers is not sufficiently reliable on its own. The current baseline therefore supplements the CSS contract with a frontend drag bridge in `frontend/src/public/ui/session-terminal-runtime-controller.js`: left-button drags that start inside the real viewport scrollbar gutter are translated back into `scrollTop` updates until mouseup, so drag remains usable without regressing terminal focus, paste, or middle-click behavior.
- The frontend mouse-forwarding-off output sanitizer must remain chunk-safe: incomplete trailing `ESC[` / CSI fragments are buffered per terminal before mouse-tracking private-mode stripping so split PTY output cannot leak raw cursor-position fragments such as `40;2H` into the rendered terminal.
- Authoritative runtime snapshots must trigger local-only terminal stabilization passes after replay so restored sessions come up visually stable after restart/reconnect: force resize plus viewport refresh must run without emitting backend resize traffic, and hidden terminals must be marked for deferred viewport sync on next reveal.
- Snapshot stabilization now also needs an explicit xterm `refresh(...)` plus one forced resize pass after `document.fonts.ready`; the earlier local-only resize/scroll sync improved restart rendering but still left some terminals partially scrambled until later interaction when late font metrics were involved.
- Hard reload / first-mount rendering also needs a per-terminal local stabilization pass, not only the global snapshot pass: visible terminals now run local-only force resize plus viewport refresh/scroll sync immediately after mount and again on the delayed mount timers so the first clean screen draw does not depend on a later click, focus, or scroll interaction.
- Snapshot replay retries for late-mounted terminals must not skip that stabilization. If a terminal receives replayed output only on a deferred retry, the retry pass must schedule the same local-only post-replay stabilization for that specific session; otherwise the first render can still stay scrambled until some later interaction even though the earlier global snapshot pass already ran.
- Manual recovery is now also part of that redraw contract: every mounted terminal header exposes a refresh action next to the terminal options button, and that action must keep calling the same local-only stabilization path (force resize with `skipRemote`, viewport refresh, scroll sync, optional follow-to-bottom) rather than inventing a second incompatible redraw implementation.
- Session-settings tabs must keep a stable dialog height across `Startup`, `Note`, and `Theme`; the delivered baseline uses one shared grid-stacked layout with active/inactive panel states so the browser preserves the tallest-panel height instead of depending only on hidden-panel remeasurement during tab switches.
- Custom dialog layout styling must not override the native hidden-state contract for `<dialog>` elements. The current baseline relies on an explicit author-side `dialog:not([open]) { display: none !important; }` rule because `.session-settings-dialog` and `.workspace-manager-dialog` intentionally set custom layout display modes (`grid`) that would otherwise make closed dialogs visible.
- The left sidebar is intentionally reduced to connection state, deck/session navigation, `New Deck` / `New Session`, `Find`, `Settings`, and `Layouts`; saved connection-profile and workspace-preset/group management now live in a dedicated secondary management surface outside the sidebar, reachable through the `Manage` entry point in the control-pane meta strip.
- The delivered management surface is a `Workspace Library` dialog with explicit `Connections` and `Workspace Presets` tabs, keeping the sidebar focused on navigation while preserving parity with the existing runtime and slash-command capabilities.
- The delivered H54 follow-up expanded that surface so connection profiles can now be created and edited directly in the dialog with a visible normalized launch payload, workspace presets now expose richer detail and duplication, and deck-group management now makes local-only versus persisted state explicit while matching the slash-command surface through `/workspace group ...`.
- The delivered H55 closeout now keeps the slash-command surface in parity with those delivered management flows:
  - `/connection ...` now supports blank profile creation, active-session draft loading, edited-draft mutation and save, detail inspection, and duplication through explicit slash subcommands.
  - `/workspace ...` now supports saved-preset detail inspection and duplication in addition to the earlier list/save/apply/rename/delete surface.
  - Operator-facing `/settings ...` is now typed and explicit across startup, note, theme, input safety, and mouse forwarding instead of relying on raw JSON mutation payloads as the documented primary path.
- The delivered H57 UX closeout makes the `Workspace Library` and related settings surfaces usable as a guided v1 instead of internal operator tooling:
  - the `Connections` tab now exposes explicit `New Local` and `New SSH` flows, a structured normalized launch form, `Save Profile` and `Save and Launch` actions, and an advanced raw launch JSON preview that is no longer the primary editing path;
  - guided SSH profile creation now keeps host, port, username, auth-method choice, runtime-secret expectations, private-key path input, and SSH host-key trust management in one visible interaction model backed by the existing backend SSH trust-entry contract;
  - the `Workspace Presets` and deck-group areas now explain saved-state semantics more clearly, while the fixed terminal `Terminal Size` / `Saved Layouts` controls and deck/session settings wording now use more self-explanatory primary action labels and inline guidance.
- The post-H57 UX hardening slice further tightened those management/settings flows:
  - primary `Workspace Library` CRUD flows for saved connection profiles, workspace presets, and deck groups no longer rely on browser `prompt()` / `confirm()` dialogs; they now use inline name inputs, inline runtime-secret entry, and inline two-step delete confirmation in the dialog itself;
  - `Save and Launch` for SSH connection-profile drafts must preserve the entered runtime secret across the intermediate save/rerender step before launching the saved profile;
  - session-settings close semantics are now intentionally unified: `Cancel`, dialog dismiss, and `Escape` all discard the draft and resync controls from persisted session state before closing.
- The delivered H58 closeout finished the remaining management/settings usability gaps:
  - `frontend/src/public/index.html` and `frontend/src/public/styles.css` now keep the `Workspace Library` body and panels scrollable inside the dialog shell instead of letting the connections surface exceed the viewport without an internal scroll path;
  - `frontend/src/public/connection-profile-runtime-controller.js` now hides SSH-only controls for `local` drafts, keeps auth-specific SSH fields conditional, and uses backend-backed host-key probing plus a fetch/review/trust workflow instead of asking operators to type raw `keyType` and public key values by hand;
  - session settings and management surfaces now keep primary end-user actions visible by default while low-level launch, theme, input-safety, and expert transport controls live behind explicit `Advanced` disclosure sections;
  - workspace preset and deck-group summaries now describe user effect rather than storage mechanics.
- The reduced left sidebar is no longer fully static: `Find`, `Terminal Size`, and `Saved Layouts` each have a header-level disclosure toggle with a persistent collapsed state stored inside `ptydeck.settings.v1`. That disclosure state is global UI preference, not part of saved layout profiles.
- The control-pane now includes a per-session send-history browser opened from the meta strip next to `Manage` / position / hide. The delivered baseline is frontend-local and bounded rather than backend-persisted: successful composer sends are stored per session in bounded local history with preview metadata, the default list renders only truncated summaries, long entries require explicit detail selection for full inspection, and selecting an entry restores it into the command input box without auto-sending it.
- The send-history follow-up has now hardened that baseline into a safer end-user workflow: the UI exposes explicit local-persistence disclosure plus `Delete Selected` / `Clear Session` hygiene controls, restore-to-input now requires confirmation before replacing a non-empty composer draft, and the dialog stays pinned to the session it was opened for until the operator explicitly switches context with `Use Current Session`.
- Any sidebar/body shell that combines `hidden` with an explicit layout display mode needs an author-side hidden contract. The current sidebar disclosure implementation depends on `.sidebar-section-body[hidden] { display: none !important; }`; without that rule, the body-level `display: grid` styling wins and the disclosure buttons appear to do nothing even though the collapsed state is changing correctly.
- Help, usage, and autocomplete should continue to be treated as part of the command surface itself so the documented slash plane stays in parity with delivered behavior.
- The H56 quality hardening closeout on 2026-04-02 delivered the identified near-term risk reductions:
  - backend session normalizers (`session-input-safety-profile.js`, `session-mouse-forwarding.js`) now have direct unit coverage for strict versus non-strict normalization and default fallback behavior;
  - frontend helper modules for share-access parsing, mouse-forwarding sequence filtering, and command-suggestion state now have dedicated direct tests instead of only indirect app/runtime coverage;
  - `connection-profile-runtime-controller.js` and `workspace-preset-runtime-controller.js` now have additional direct coverage for update, cancellation, normalization, and local-only edge paths;
  - the generated `theme-library.js` catalog now has dedicated integrity coverage for unique IDs, required color keys, and normalized hex payload shape;
  - `app-runtime-composition-controller.js` now uses an extracted DOM-ref collection seam (`app-runtime-dom-refs.js`) with direct contract coverage instead of keeping that selector contract buried only inside broad app tests.

## Quality and Operational Rules

- Keep markdown content in US English.
- Do not mark work as done before implementation, validation, and documentation sync are complete.
- Close implementation tasks with the local quality gate unless a narrower documented equivalent is explicitly agreed for the scope: `npm run lint`, `npm run test`, `npm run test:coverage:check`.
- Do not leave orphan validation or background processes behind.
- Major architecture changes require explicit confirmation by `SAS`.

## Deferred Theme Clusters

- Security and multi-tenancy
- Scale and runtime isolation
- Extensibility
- Technical alternatives and stack evolution
- Remote / external theme compatibility (`REM-008A`, `REM-008B`, `REM-008C`)
