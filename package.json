{
  "name": "ptydeck",
  "version": "0.2.0",
  "private": true,
  "description": "Monorepo for ptydeck backend and frontend.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "bash -lc 'npm --prefix backend run dev & npm --prefix frontend run dev & wait'",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "scripts:log:check": "./scripts/check-script-logging.sh",
    "lint": "npm run scripts:log:check && ./scripts/check-adr-process.sh && npm --prefix backend run lint && npm --prefix frontend run lint",
    "test:root": "node --test test/*.test.js",
    "test": "npm run test:root && npm --prefix backend run test && npm --prefix frontend run test",
    "test:coverage": "npm --prefix backend run test:coverage && npm --prefix frontend run test:coverage",
    "test:load": "npm --prefix backend run test:load",
    "runtime:profile:check": "./scripts/check-runtime-profile.sh",
    "test:coverage:check": "./scripts/check-coverage.sh",
    "tls:check": "./scripts/check-cert-expiry.sh",
    "security:sca": "./scripts/security-scan.sh",
    "security:sbom": "./scripts/generate-sbom.sh",
    "backup:create": "./scripts/backup-sessions.sh",
    "backup:restore": "./scripts/restore-sessions.sh",
    "backup:verify": "./scripts/verify-backup-restore.sh",
    "dr:drill": "./scripts/dr-restore-drill.sh",
    "retention:purge": "./scripts/purge-retention.sh",
    "release:evidence": "./scripts/generate-release-evidence.sh"
  }
}
